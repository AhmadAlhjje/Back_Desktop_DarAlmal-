/**
 * حضور الجلسات (قرار المستخدم 2026-09-22): الحساب لا يُفتح من حاسوبين معاً. «مفتوح» = له بثّ SSE
 * حيّ من جهاز؛ إغلاق التطبيق يقطع البثّ فيتحرّر الحساب خلال ثوانٍ. المعرّف الجهاز (deviceId) في
 * التوكن، فالجهاز نفسه لا يحجب نفسه (إعادة تشغيل سريعة).
 */
export interface SessionPresence {
  /** هل للإداري جلسة حيّة على جهاز آخر غير [deviceId]؟ (deviceId غير معروف يُعدّ جهازاً مختلفاً). */
  isActiveElsewhere(adminId: string, deviceId: string | undefined): boolean;
}
